namespace day01

countIncreases : [Int] -> Int
countIncreases lst =
    slidingPairs lst |> List.count (cases (a, b) -> b > a) |> toInt

main : '{IO, Exception} ()
main = do
    input = readLines "input01.txt" |> toInts
    n1 = input |> countIncreases
    printLine ("Solution 1: " ++ (toText n1))
    n2 = input |> slidingWindow 3 |> map sumInts |> countIncreases
    printLine ("Solution 2: " ++ (toText n2))
